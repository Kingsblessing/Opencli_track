# 主要参考

当前的正则匹配方法之所以精准度极低，根本原因在于**匹配词库混淆了“需求方（买家/客户）”与“供给方（卖家/陪陪）”的表达差异**。现有的“接陪、代打、代练、接单”极大概率匹配到的是同行宣发，而非目标客户。

**现行方案的缺陷分析**

* **身份错位**：“接单、接陪、代打”属于陪玩或打手的营销词汇，匹配到的是竞争对手而非消费者。
* **泛化噪音过高**：“陪、技术”属于高频通用词（如“这技术不行”、“陪我逛街”），单字匹配误报率极高。
* **缺少需求意图**：没有结合“寻求/祈使”动词（如“求”、“找”、“缺”）。

---

**改进方案：基于意图组合与反向过滤的正则匹配**

寻找目标客户（买家）的核心，是捕获**寻求服务**的表达模式，建议采用“**需求动作 + 服务词**”的组合匹配，并配合**排除规则**。

| 匹配类型 | 提取逻辑 | 正则表达式示例 | 匹配示例评论 |
| --- | --- | --- | --- |
| **强意图买家** | 寻求动词 + 陪玩/付费词汇 | `(?=.*(找|求|来|缺|有无|需要|想找|请))(?=.*(陪玩|陪陪|付费|有偿|车头|导师))` | “求一个无畏契约陪玩，晚上能打的” |
| **弱意图求带** | 寻求动词 + 求带/组队词汇 | `(?=.*(求|找|缺|来|有无|救救))(?=.*(带带|带我|拉我|固定队|车位|上分))` | “缺个大佬带带我，总是连败” |
| **疑问寻源** | 询问是否有服务 | `(谁|有人|有无|还有|有木有).*(接单|接陪|带|陪玩|开车)` | “现在还有人接陪玩吗？求推荐” |
| **负向排除词（卖家）** | 匹配到直接丢弃 | `.*(私我|主页|联系我|报价|主玩|价格|接单|有偿接|工作室).*` | “主页看报价，可接代练” （过滤） |

---

**自动化寻客的算法优化建议**

* **构建双向过滤流水线 (Pipeline)**
1. **正向正则提取**：使用“强/弱意图”正则筛选出初步评论候选集。
2. **负向正则清洗**：二次运行“卖家关键词”过滤，直接剔除同行自荐评论。


* **主贴上下文分类控制**
* **需求型主贴**（如“找个无畏契约陪玩”）：评论区多为同行，真正的**目标客户是发帖人**。
* **炫技/内容型主贴**（如“赋能局五杀集锦”）：评论区的“求带”、“拉我”、“有无陪玩”才是真正具备消费意向的**目标客户**。

# 次要参考

我把这份表按“**现有正则到底抓到了什么、漏掉了什么、为什么会误抓、怎样改成真正面向客户购买意图的算法**”拆了一遍。结论其实比较明确：

> **目前最大的瓶颈不是关键词太少，而是把“陪玩相关词”错误地当成了“潜在客户意图”。**
>
> 对自动找客来说，应该从“关键词匹配”升级为 **候选召回 → 意图识别 → 排除供给方 → 联系优先级排序**。

## 1. 先看这份数据暴露出的核心问题

表中共 **974 条评论**。

当前 `matched_patterns` 非空的有：

* **254 条 / 974 条 ≈ 26.1%**
* 未命中 **720 条 ≈ 73.9%**

当前原始关键词的实际命中情况大致是：

| 关键词 | 出现次数 |
| --- | ---: |
| 陪   |  206 |
| 陪玩  |   65 |
| 技术  |   54 |
| 陪陪  |   27 |
| 接单  |   20 |
| 代打  |   14 |
| 代练  |    1 |
| 接陪  |    1 |

这里第一个非常明显的问题就是：

### “陪”这个词信息量太低

例如：

> “来个陪”
> “有没有女陪吗”
> “找个男陪”

确实非常有价值。

但同样会抓到大量：

> “男大陪 20/h”
> “历史神话青年音”
> “希望可以陪到你”
> “下午5点以后双神话陪”
> “技术陪 40/h”
> “赋能陪……”

这些实际上是**陪玩提供方在自我宣传**。

也就是说：

```text
“陪”
```

只能告诉你：

> 这条评论和陪玩有关。

却不能告诉你：

> 这个人是不是买家。

---

# 2. 真正严重的问题：大量客户意图根本没有命中

这是这份数据里最值得关注的一点。

我专门检查了 **当前没有命中的评论**，发现大量非常明显的潜在客户表达。

### 第一类：下单意图

比如：

> “怎么点”

> “怎么点单”

> “问问怎么点”

> “现在能点单么”

> “点单”

> “怎么下单”

这些实际上是整个数据集里**价值非常高的客户信号**。

而其中：

* “怎么点”一共出现 **21 次**
* **21 次全部没有被当前正则命中**

也就是说：

```text
怎么点
```

在你的当前关键词体系里完全是漏网之鱼。

---

### 第二类：询问是否还有人接单

例如：

> “现在有人接吗”

> “现在接吗”

> “还接吗”

> “新手接吗”

> “还能点嘛”

> “现在接不接”

这种表达其实比单纯出现“陪”更加有价值。

因为它表达的是：

```text
我要找服务
    ↓
现在有没有人提供
    ↓
准备成交
```

而不是单纯聊陪玩。

例如数据中：

```text
现在有人接吗
现在接吗
还接吗
新手接吗
```

都没有被目前的正则有效识别。

---

# 3. 建议不要再把关键词设计成一组，而是分成“意图词”

这是最重要的改法。

不要：

```text
陪玩|陪陪|接陪|代打|代练|接单|陪|技术
```

而应该分成至少 **5 个词库**。

---

# 4. 第一组：购买/咨询行为词 —— 最重要

这一组才是你真正应该重点抓的。

### A. 下单类

建议增加：

```text
点单
点陪
下单
怎么点
怎么点单
怎么下单
怎么约
怎么订
怎么找
```

可以写成：

```regex
(?:点单|点陪|下单|怎么点|怎么点单|怎么下单|怎么约|怎么订)
```

更加宽松：

```regex
(?:怎(?:么|麼).{0,2}(?:点|訂|订|下单)|点(?:单|陪)|下(?:单|陪))
```

---

### B. “有没有人接”类

这是第二重要的一组：

```text
有人接吗
有人接么
现在接吗
现在接么
还接吗
还接么
能接吗
可以接吗
新手接吗
今天接吗
晚上接吗
明天接吗
```

可以用：

```regex
(?:现在|目前|今天|今晚|明天|晚上|明早)?(?:有人|能|可以|还)?接(?:吗|么|不|吗)
```

再补一些实际口语：

```regex
(?:还|现在|目前|今天|今晚)?(?:有|能有)(?:吗|么)?
```

不过这一类不建议只用一个超宽正则，否则：

```text
你们招人吗
```

也可能被误判。

---

# 5. 第二组：明确“找陪玩”的需求词

建议增加：

```text
找陪
找个陪
来个陪
来个女陪
来个男陪
有没有陪
有陪吗
需要陪
想找陪
想要陪
陪我
找陪玩
```

核心规则：

```regex
(?:找|求|来个|来一(?:个|位)?|要个|需要|想找|想要|有没有|有)(?:.{0,8})(?:陪玩|陪陪|女陪|男陪|技术陪|娱乐陪|陪)
```

例如可以抓：

```text
来个女陪
有没有陪
找个男陪
有没有技术陪
需要一个陪
想找个娱乐陪
```

但是有一个非常重要的原则：

> **不要再把“陪”单独作为高权重关键词。**

可以保留“陪”作为召回词，但是它应该是**低权重召回条件**。

---

# 6. 第三组：价格咨询词

这类词很有价值，但是不能单独使用。

例如：

> “多少钱一小时”

> “多少一把”

> “什么价格”

> “啥价格”

> “多少一把啊”

建议：

```regex
(?:多少(?:钱)?|什么价格|啥价格|价格多少|怎么收费).{0,8}(?:一小时|一把|一局|陪|陪玩)?
```

以及：

```regex
(?:一小时|一把|一局).{0,3}(?:多少|多少钱|什么价格|啥价格)
```

因为：

```text
这价格真的假的？
```

不一定是客户。

但是：

```text
多少钱一把呀
```

就非常接近直接购买意图。

---

# 7. 第四组：游戏需求词

这组对无畏契约尤其重要。

数据中出现了大量：

```text
带我上黄金
带我上分
上超凡
晋级赛
钻石晋超凡
神话
赋能
包赢
```

但是这里不能简单地：

```regex
神话|赋能|包赢
```

因为这些词大量存在于**陪玩广告**里。

应该匹配：

```text
带我……
帮我……
上……
晋级……
```

例如：

```regex
(?:带我|带一下|帮我|陪我).{0,10}(?:上分|上黄金|上铂金|上钻石|上超凡|上神话)
```

以及：

```regex
(?:上|冲|晋级).{0,6}(?:黄金|铂金|钻石|超凡|神话)
```

但这一组建议权重低于“怎么点”“接吗”。

---

# 8. 第五组：自然语言弱意图

还有一类特别容易被漏掉：

> “一起玩吗”

> “可以一起玩”

> “没人一起玩”

> “找不到人”

> “有人吗”

> “陪我玩”

这类评论不能直接当成付费客户。

例如：

> “我是黑铁，可以跟我一起玩”

很可能只是**免费找队友**。

所以：

```text
一起玩
```

应该属于：

> 弱意图

而不是：

> 强客户意图。

---

# 9. 最关键的改进：建立“正向意图 + 负向供给”的双向规则

这是我最推荐你的算法结构。

不要再做：

```text
匹配关键词 → 潜在客户
```

改成：

```text
匹配关键词
      ↓
是否有购买意图？
      ↓
是否像陪玩提供方？
      ↓
综合评分
      ↓
高意向 / 中意向 / 低意向 / 排除
```

---

# 10. 专门建立一个“卖家/陪玩提供方”词库

这是目前系统最大的缺口。

例如：

```text
本人
我是
主页
私信
试音
接单记录
战绩
可验枪
可验号
秒上号
首单
首局
价格
/h
一把
一局
情绪价值
声音
青年音
少女音
神话
赋能
在线等单
全天在线
蹲板板
希望可以陪到
欢迎来
欢迎私信
可教学
包c
包赢
输前三
```

另外还有非常典型的：

```text
招陪
招人
还招人吗
俱乐部
进团
入团
新店
营业
平移
```

这些通常代表：

> 招募陪玩 / 陪玩本人 / 俱乐部

而不是你的目标客户。

---

# 11. “接单”其实也是双向词

比如：

> “现在有人接单吗”

是潜在客户。

但是：

> “我一般接单就是……”

是陪玩。

所以不能：

```regex
接单
```

而应该：

### 客户方向

```regex
(?:现在|今天|今晚|明天|晚上)?(?:有人)?接单(?:吗|么|不)
```

### 供给方向

```regex
(?:我|本人|一直|目前|平时).{0,5}接单
```

---

# 12. “技术”也不能作为独立客户关键词

数据里已经非常明显：

### 高噪声

```text
娱乐技术 什么声线都有 来试试吗哥哥
```

```text
技术陪 40/h
```

```text
技术好服务好
```

```text
技术娱乐都招
```

这绝大部分是供给方。

但是：

> “有没有技术女陪钻石”

又明显是需求。

所以应该匹配：

```regex
(?:有没有|有|找|来个|需要|想找).{0,6}技术(?:女陪|陪)?
```

而不是：

```regex
技术
```

---

# 13. “代打”也是一样

例如：

> “有没有靠谱代打呀，现在来”

这是明显需求。

而：

> “无畏契约代打默认直播……”

是卖家。

所以：

```regex
(?:有没有|有没|找|求|需要|谁有|哪里有).{0,8}(?:代打|代练)
```

比：

```regex
代打|代练
```

有效得多。

---

# 14. 我建议你把正则改成“召回正则”

第一阶段不要追求极高精度。

例如：

```python
import re

patterns = {
    "order": r"""
        (?:点单|点陪|下单|怎么点|怎么点单|怎么下单|怎么约|怎么订)
    """,

    "availability": r"""
        (?:
            现在|目前|今天|今晚|明天|晚上|明早
        )?
        (?:
            有人|能|可以|还
        )?
        接(?:吗|么|不)?
    """,

    "looking_for": r"""
        (?:
            找|求|来个|来一[个位]?|要个|需要|想找|想要|有没有|有
        )
        .{0,8}
        (?:
            陪玩|陪陪|女陪|男陪|技术陪|娱乐陪|陪
        )
    """,

    "price_query": r"""
        (?:
            多少(?:钱)?|多少钱|什么价格|啥价格|价格多少|怎么收费
        )
        .{0,8}
        (?:
            一小时|一把|一局|陪|陪玩
        )?
    """,

    "game_demand": r"""
        (?:
            带我|带一下|帮我|陪我
        )
        .{0,10}
        (?:
            上分|上黄金|上铂金|上钻石|上超凡|上神话|晋级赛
        )
    """,

    "代打需求": r"""
        (?:
            有没有|有没|找|求|需要|谁有
        )
        .{0,8}
        (?:代打|代练)
    """
}
```

这里有一个非常重要的设计：

> **正则只负责召回，不负责最终判定。**

---

# 15. 第二阶段做“意图评分”

我比较推荐你直接做一个评分模型。

例如：

| 特征             |   分值 |
| -------------- | ---: |
| “怎么点/怎么下单”     |   +5 |
| “有人接吗/现在接吗”    |   +5 |
| “找个陪/有没有陪”     |   +4 |
| “多少钱一把”        |   +4 |
| “需要技术陪”        |   +4 |
| “带我上分”         |   +3 |
| “陪我”           |   +3 |
| “一起玩”          |   +1 |
| 单独出现“陪”        | +0.5 |
| “我是陪玩/本人……”    |   -5 |
| “主页/私信/试音”     |   -4 |
| “招陪/招人/进团”     |   -5 |
| “首单/优惠/价格表”    |   -4 |
| “青年音/少女音/情绪价值” |   -3 |
| “蹲板板”          |   -4 |
| 大段价格表          |   -5 |
| “俱乐部” + 招募语句   |   -5 |

最终：

```text
score >= 6      高意向
3 <= score < 6  中意向
1 <= score < 3  弱意向
score < 1       排除
```

这里不是让“分数”成为最终判断，而是方便你后续人工查看和自动排序。

---

# 16. 更进一步：一定要做“客户 / 提供者 / 无关”三分类

我认为这才是这个项目真正应该实现的目标。

最终每条评论不应该只有：

```text
matched_patterns
```

而应该有：

```text
intent
intent_score
matched_positive
matched_negative
lead_level
reason
```

例如：

| text         | intent | score | reason     |
| ------------ | ------ | ----: | ---------- |
| 怎么点          | 客户     |     8 | 明确下单咨询     |
| 现在有人接吗       | 客户     |     8 | 明确询问服务是否可用 |
| 有没有技术女陪钻石    | 客户     |     9 | 明确寻找特定陪玩   |
| 多少钱一把呀       | 客户     |     7 | 价格咨询       |
| 娱乐女陪20/h欢迎私信 | 提供者    |    -5 | 自我营销       |
| 主页有战绩，可试音    | 提供者    |    -6 | 服务提供方      |
| 一起玩吗         | 弱意向    |     1 | 可能是免费组队    |
| 陪玩接单很累       | 无关     |    -3 | 讨论陪玩行业     |

这样才真正接近你的业务目标。

---

# 17. 这份数据特别适合做“评论者角色识别”

你还有一个非常好的数据字段：

```text
author
```

一定不要浪费。

比如某个用户：

```text
评论A：怎么点
评论B：多少钱
评论C：现在有人接吗
```

这个账号非常可能是潜在客户。

反过来：

```text
评论A：15/h
评论B：欢迎私信
评论C：可试音
评论D：主页有战绩
评论E：接单记录
```

这个账号几乎就是提供方。

所以可以增加：

```text
author_level
```

按照作者历史评论统计：

```text
seller_probability
customer_probability
```

这比单条评论判断强很多。

---

# 18. 还有一个非常明显的问题：重复评论

你的数据中：

* 974 条原始评论
* 去重后约 **768 条**
* 有 **206 条重复**

而且有一些非常典型的重复：

```text
“私信我”        28 次
“还招人吗”       17 次
“怎么点”         14 次
“1”              10 次
“招陪～找板～新店开业...” 10 次
```

这里说明：

> **同一个陪玩/机构可能在多个帖子下面批量刷评论。**

所以建议做：

```text
exact duplicate
↓
author + normalized_text
↓
同作者近似重复
```

例如：

```text
“怎么点”
“怎么点啊”
“怎么点呀”
“怎么点单”
```

可以通过文本相似度归并。

否则你以为找到了 20 个客户，实际可能只有 5 个人。

---

# 19. 帖子标题也应该进入算法

这一点非常重要。

现在你的数据已经有：

```text
video_title
```

但匹配算法显然没有充分利用。

例如：

### 高价值商业帖子

```text
瓦陪9.9h
瓦点单｜乖乖电竞性价比封神
有无瓦陪店！！
```

这种帖子下面：

```text
怎么点
现在接吗
多少钱
有没有技术陪
```

可信度非常高。

---

而：

```text
粉丝投稿
转账完被拉黑
避雷无畏契约陪
这价格也太高了吧…
```

里面大量评论只是：

> 讨论、吐槽、复述经历、陪玩行业交流。

这些即使出现：

```text
陪
代打
接单
技术
```

也不应该直接成为客户。

所以可以先给帖子做分类：

```text
POST_TYPE
├── 服务售卖
├── 用户求陪
├── 讨论
├── 避雷
└── 无关
```

然后给不同类型帖子使用不同权重。

---

# 20. 我最终推荐的完整架构

如果你准备把这个真正做成自动化程序，我建议：

```text
小红书搜索
   │
   ├── 无畏契约
   ├── 陪玩
   ├── 瓦陪
   ├── 瓦点单
   ├── 代打
   └── 技术陪
          ↓
      获取帖子
          ↓
      获取评论
          ↓
       文本清洗
          ↓
        去重
          ↓
    ┌───────────────┐
    │ 第一阶段召回   │
    │ Regex / 关键词 │
    └───────────────┘
          ↓
      候选评论池
          ↓
    ┌────────────────────┐
    │ 第二阶段意图识别    │
    │ 客户/供给/无关       │
    └────────────────────┘
          ↓
    ┌────────────────────┐
    │ 第三阶段客户打分     │
    │ 购买意图 + 游戏需求  │
    │ 作者历史行为         │
    │ 帖子上下文           │
    └────────────────────┘
          ↓
      Lead Score
          ↓
  ┌───────┼────────┐
 高意向   中意向    排除
```

---

# 21. 如果你现在只想“改正则”，我建议先改成这个版本

先不要急着上 LLM。

你的第一版可以直接采用：

### 一级：强客户意图

```regex
(?:怎么点|怎么点单|怎么下单|点单|点陪|下单|怎么约|怎么订|
(?:现在|目前|今天|今晚|明天)?(?:有人|能|可以|还)?接(?:吗|么|不)|
(?:找|求|来个|来一[个位]?|要个|需要|想找|想要|有没有|有).{0,8}(?:陪玩|陪陪|女陪|男陪|技术陪|娱乐陪)|
(?:多少(?:钱)?|多少钱|什么价格|啥价格|价格多少|怎么收费).{0,8}(?:一小时|一把|一局|陪|陪玩)|
(?:带我|带一下|帮我|陪我).{0,10}(?:上分|上黄金|上铂金|上钻石|上超凡|上神话|晋级))
```

### 二级：辅助关键词

```regex
陪玩|陪陪|陪|代打|代练|技术|赋能|上分
```

### 三级：排除提供方

```regex
(?:我是陪玩|本人.*陪|招陪|招人|进团|入团|俱乐部|主页.*战绩|
可试音|免费试音|接单记录|欢迎私信|蹲板板|希望可以陪到|
首单|优惠|新店|营业|在线等单)
```

**最终不要是“命中任意一个词 = 客户”，而应该是：**

```text
强客户词命中
    +
没有明显卖家词
    ↓
高优先级

只有“陪/技术/代打”
    ↓
不要直接判断

只有卖家词
    ↓
排除
```

---

# 22. 从这份数据来看，最值得优先增加的词

按照实际漏检价值，我会优先增加：

```text
怎么点
怎么点单
点单
点陪
怎么下单
下单
有人接吗
现在接吗
还接吗
能接吗
新手接吗
怎么约
多少钱一把
多少钱一小时
多少一把
有陪吗
找个陪
来个陪
需要陪
陪我
有没有技术陪
有没有女陪
找代打
有没有代打
```

而下面这些**不要继续提高权重**：

```text
陪
技术
接单
代打
代练
赋能
神话
老板
板板
价格
```

它们更适合作为**上下文特征**，不适合作为独立客户判据。

---

# 23. 如果要把准确率真正做到可用，我建议下一步直接做机器学习/LLM二阶段

你现在已经有了非常好的训练数据雏形。

人工标注大约 **300～500 条**就可以开始做：

```text
0 = 无关
1 = 陪玩提供方
2 = 潜在客户
```

然后先做一个很轻量的 baseline：

```text
中文文本
 ↓
TF-IDF
 ↓
Logistic Regression
 ↓
客户概率
```

这个方案非常适合先验证“机器学习到底是否比正则好”。

后面再升级：

```text
BGE / Sentence-BERT embedding
        ↓
客户 / 提供方分类器
        ↓
规则 + embedding ensemble
```

甚至结合你现在的本地 LLM：

```text
Regex 高召回
        ↓
Embedding 初筛
        ↓
本地 LLM 判断意图
        ↓
Lead Score
```

这会比单纯堆几十个关键词稳定得多。

---

## 最后给你一个非常具体的结论

你的当前方法实际上解决的是：

> **“这条评论是否谈到了陪玩相关内容？”**

但你的真实业务目标是：

> **“这条评论的作者有没有产生购买无畏契约陪玩服务的意图？”**

这两个任务不是一回事。

因此下一版最好把字段从：

```text
matched_patterns
```

升级成：

```text
positive_patterns
negative_patterns
intent
intent_score
lead_level
reason
```

例如最终输出：

```text
评论：怎么点
positive_patterns：怎么点
negative_patterns：
intent：customer
intent_score：9
lead_level：HIGH
reason：明确询问下单方式
```

以及：

```text
评论：本人神话二，青年音，可试音，主页有战绩
positive_patterns：
negative_patterns：本人|青年音|试音|主页|战绩
intent：provider
intent_score：-8
lead_level：EXCLUDE
reason：典型陪玩自我营销
```

这样你的程序就不再是一个“关键词爬虫”，而会开始变成一个真正的**潜在客户识别系统**。
